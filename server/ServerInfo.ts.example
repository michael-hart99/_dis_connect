export const HOST = /* www.mydomain.com */;

export const PORT = /* The port to run sockets over */;

export const SSL_CERT = /* /path/to/www/mydomain.com/SSL/cert.pem */;
export const SSL_KEY = /* /path/to/www/mydomain.com/SSL/key.pem */;

export interface StateInfo {
  state: string;
  originTime: number | null;
}
export type PacketData =
  | number
  | StateInfo
  | RTCIceCandidateInit
  | RTCSessionDescriptionInit
  | null;

export interface ServerMessage {
  from: string;
  to: string;
  action: string;
  data: PacketData;
}
